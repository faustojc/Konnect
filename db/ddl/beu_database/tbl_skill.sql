CREATE TABLE beu_database.tbl_skill
(
    id                INT AUTO_INCREMENT
        PRIMARY KEY,
    skill             VARCHAR(255) NOT NULL,
    skill_description VARCHAR(255) NOT NULL
)
    CHARSET = latin1;
